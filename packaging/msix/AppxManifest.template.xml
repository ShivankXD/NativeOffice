<?xml version="1.0" encoding="utf-8"?>
<!--
  MSIX manifest template for NativeOffice. build-msix.ps1 substitutes the
  {{...}} placeholders; never hand-edit the staged copy, edit this template.

  Store submissions: Identity Name / Publisher / PublisherDisplayName MUST
  exactly match the values Partner Center shows under
  Product management -> Product identity. The placeholder defaults are only
  valid for local self-signed sideload testing.
-->
<Package xmlns="http://schemas.microsoft.com/appx/manifest/foundation/windows10"
         xmlns:uap="http://schemas.microsoft.com/appx/manifest/uap/windows10"
         xmlns:rescap="http://schemas.microsoft.com/appx/manifest/foundation/windows10/restrictedcapabilities"
         IgnorableNamespaces="uap rescap">

  <Identity Name="{{IDENTITY_NAME}}"
            Publisher="{{PUBLISHER}}"
            Version="{{VERSION}}"
            ProcessorArchitecture="x64" />

  <Properties>
    <DisplayName>NativeOffice</DisplayName>
    <PublisherDisplayName>{{PUBLISHER_DISPLAY}}</PublisherDisplayName>
    <Logo>Assets\StoreLogo.png</Logo>
  </Properties>

  <Dependencies>
    <!-- 17763 = Windows 10 1809, the usual floor for full-trust MSIX. -->
    <TargetDeviceFamily Name="Windows.Desktop"
                        MinVersion="10.0.17763.0"
                        MaxVersionTested="10.0.26100.0" />
  </Dependencies>

  <Resources>
    <Resource Language="en-us" />
  </Resources>

  <Capabilities>
    <!-- Classic Win32 process: full user-level trust inside the container. -->
    <rescap:Capability Name="runFullTrust" />
  </Capabilities>

  <Applications>
    <Application Id="NativeOffice"
                 Executable="NativeOffice.exe"
                 EntryPoint="Windows.FullTrustApplication">

      <uap:VisualElements DisplayName="NativeOffice"
                          Description="Create, analyze and present — word processing, spreadsheets, presentations, PDF and Markdown in one native office suite."
                          BackgroundColor="transparent"
                          Square150x150Logo="Assets\Square150x150Logo.png"
                          Square44x44Logo="Assets\Square44x44Logo.png">
        <uap:DefaultTile Wide310x150Logo="Assets\Wide310x150Logo.png"
                         Square71x71Logo="Assets\Square71x71Logo.png"
                         Square310x310Logo="Assets\Square310x310Logo.png" />
        <uap:SplashScreen Image="Assets\SplashScreen.png"
                          BackgroundColor="#FFFFFF" />
      </uap:VisualElements>

      <Extensions>
        <!-- nativeoffice:// (auth-callback fast path). Replaces the HKCU
             Software\Classes registration InstanceGuard writes for the
             unpackaged build; inside the container that write is virtualized
             and invisible to the browser, so the manifest declaration is the
             one that counts. The URL arrives as a command-line argument. -->
        <uap:Extension Category="windows.protocol">
          <uap:Protocol Name="nativeoffice">
            <uap:DisplayName>NativeOffice</uap:DisplayName>
          </uap:Protocol>
        </uap:Extension>

        <!-- Own document format: default handler. -->
        <uap:Extension Category="windows.fileTypeAssociation">
          <uap:FileTypeAssociation Name="noff">
            <uap:DisplayName>NativeOffice Document</uap:DisplayName>
            <uap:SupportedFileTypes>
              <uap:FileType>.noff</uap:FileType>
            </uap:SupportedFileTypes>
          </uap:FileTypeAssociation>
        </uap:Extension>

        <!-- Formats the app can open: shows NativeOffice in Open With without
             stealing existing defaults (Windows never silently reassigns). -->
        <uap:Extension Category="windows.fileTypeAssociation">
          <uap:FileTypeAssociation Name="officedocs">
            <uap:DisplayName>Office Documents</uap:DisplayName>
            <uap:SupportedFileTypes>
              <uap:FileType>.docx</uap:FileType>
              <uap:FileType>.xlsx</uap:FileType>
              <uap:FileType>.pptx</uap:FileType>
              <uap:FileType>.csv</uap:FileType>
              <uap:FileType>.md</uap:FileType>
              <uap:FileType>.pdf</uap:FileType>
              <uap:FileType>.html</uap:FileType>
            </uap:SupportedFileTypes>
          </uap:FileTypeAssociation>
        </uap:Extension>
      </Extensions>
    </Application>
  </Applications>
</Package>
